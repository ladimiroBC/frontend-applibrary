import { Routes } from "@angular/router";
import { AppManagementBooks } from "./view/management.books.component";

export const appBooks: Routes = [
  { path: 'management-books', component: AppManagementBooks }
]
