import { Routes } from "@angular/router";
import { BookCatalogComponent } from "./view/book.catalog.component";

export const appBookCatalog: Routes = [
  { path: 'book-catalog', component: BookCatalogComponent }
]
