import { Routes } from "@angular/router";
import { AppManagementStudents } from "./view/management.students.component";

export const appStudents: Routes = [
  { path: 'management-students', component: AppManagementStudents }
]
